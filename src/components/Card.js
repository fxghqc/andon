import styled from 'react-emotion';

const Card = styled('div')`
  background-color: rgba(0, 0, 0, .4);
  border: .6rem solid rgb(36,31,25);
  border-radius: .6rem;
  box-shadow: inset 0 0 12px 0px rgba(0, 0, 0, .9), 0 0 12px 0px rgba(0, 0, 0, 0.8);
`;

export default Card;

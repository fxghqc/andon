import React from 'react';
import styled from 'react-emotion';

const BreadSplitor = styled('div')`
  display: inline-block;
  padding: 0 .5rem;
`;

export default () => (
  <BreadSplitor>
    /
  </BreadSplitor>
);
